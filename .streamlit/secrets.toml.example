# Google Cloud Service Account Credentials Template
#
# INSTRUCTIONS:
# 1. Copy this file to "secrets.toml" (remove .example)
# 2. Fill in your actual Google Cloud service account credentials
# 3. NEVER commit secrets.toml to git (it's in .gitignore)
#
# To get these credentials:
# 1. Go to Google Cloud Console (console.cloud.google.com)
# 2. Create a new project or select existing
# 3. Enable Google Sheets API and Google Drive API
# 4. Create a service account (IAM & Admin > Service Accounts)
# 5. Create a JSON key for the service account
# 6. Copy the values from the JSON key into this file
# 7. Share your Google Sheet with the service account email

[gcp_service_account]
type = "service_account"
project_id = "your-project-id-here"
private_key_id = "your-private-key-id-here"
private_key = "-----BEGIN PRIVATE KEY-----\nYour-private-key-content-here\n-----END PRIVATE KEY-----\n"
client_email = "your-service-account-name@your-project-id.iam.gserviceaccount.com"
client_id = "your-client-id-here"
auth_uri = "https://accounts.google.com/o/oauth2/auth"
token_uri = "https://oauth2.googleapis.com/token"
auth_provider_x509_cert_url = "https://www.googleapis.com/oauth2/v1/certs"
client_x509_cert_url = "https://www.googleapis.com/robot/v1/metadata/x509/your-service-account-name%40your-project-id.iam.gserviceaccount.com"

# IMPORTANT NOTES:
# - Keep the private_key as one line with \n for line breaks
# - Do not add extra quotes or spaces
# - The client_email is what you'll share your Google Sheet with (as Editor)
# - Make sure both Google Sheets API and Google Drive API are enabled in your project
